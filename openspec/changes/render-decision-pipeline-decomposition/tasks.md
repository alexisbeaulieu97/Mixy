## 1. Render-Decision Decomposition

- [x] 1.1 Refactor `build_render_decisions(...)` into smaller source-level and file-level planning helpers
- [x] 1.2 Keep the shared planning API and `RenderDecisionMap` contract stable while improving internal cohesion
- [x] 1.3 Preserve `_render_output_relative_path(...)` behavior and keep output-relative path planning close to rendered-file decision assembly

## 2. Validation

- [x] 2.1 Add or update focused tests that cover recursive metadata planning and output-relative path behavior
- [x] 2.2 Run the relevant planning and generation tests and update this task list to reflect completion
