## ADDED Requirements

### Requirement: Active OpenSpec artifacts stay aligned with runtime
Active OpenSpec specs and current user docs SHALL describe the behavior implemented by the current runtime.

#### Scenario: Reading an active spec for a current feature
- **WHEN** a contributor consults an active spec or current doc page
- **THEN** the artifact describes the supported runtime behavior rather than a stale historical contract
