# variables:
#   project_name: "MyAwesomeApp"
#   author_name: "Mixy User"
#   python_version: "3.10"

templates:
  - source: ./template-readme # Path to a local directory
  - source: ./template-python # Path to another local directory

mandatory_variables:
  - project_name # This one has a default
  - license      # This one doesn't have a default, user MUST provide it
  - author_name  # This has a default, but we make it mandatory anyway